export class Project {
  constructor(public id: number, public name: string, public description: string, public punctuation: number,
              public status: string, public creator: string, public duration: string) { }
}
